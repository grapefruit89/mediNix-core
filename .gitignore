# Nix build artifacts
/result
result
*.drv

# Nix store references (should not be committed)
/nix/

# Secrets / credentials
*.key
*.age
secrets/
*.env

# Editor / OS
.DS_Store
*.swp
*~

# Local overrides (portable core must stay clean)
compat-my.nix
my-*.nix

# Python venv / scripts (if accidentally placed here)
__pycache__/
*.pyc
venv/
second-brain.sqlite

*.log

# Unraid migration export: contains API keys + WireGuard private key — NEVER commit (public repo!)
Unraid Settings export/

# Local scratch / desktop artifacts / backups
.directory
Textdatei.txt
*.bak
